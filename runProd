#!/usr/bin/env bash
set -euo pipefail

export OPENAI_BASE_URL="${OPENAI_BASE_URL:-http://192.168.0.37:11434}"
export OPENAI_MODEL="${OPENAI_MODEL:-gpt-oss:20b}"

docker compose -f docker-compose.prod.yml up --build -d --remove-orphans
docker compose -f docker-compose.prod.yml logs -f app
