#!/usr/bin/env bash
set -euo pipefail

export OPENAI_BASE_URL="${OPENAI_BASE_URL:-https://llm.lelabs.tech:21434}"
export OPENAI_MODEL="${OPENAI_MODEL:-ornith:9b}"

docker compose -f docker-compose.yml up --build --remove-orphans
